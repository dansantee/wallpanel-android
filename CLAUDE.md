# CLAUDE.md

Claude Code reads this file automatically on startup; it does not read AGENTS.md on its own. The
line below imports the shared operating rules so they are loaded into context.

@AGENTS.md
